提供用户登录、注册的接口和页面（注册的用户都是普通用户 roleId = 3 && is_active = 0）

用户注册之后不能直接使用，需要超级管理员激活（is_active = 1）之后才能使用

// 在数据库中为一个用户的角色（role）更改为超级管理员

使用 seed 为 users 表添加一条数据（超级管理员）

有且只有超级管理员可以进入用户管理的界面 更改其他用户的角色（role）

role 分为三种 
 
1. 超级管理员（roleId = 1） 可以增删改查 可以进入用户管理界面编辑其他用户的角色

2. 管理员（roleId = 2） 可以增删改查

3. 用户（roleId = 3） 只可以查




一篇博客有多条评论
一对多
一个角色有多个用户
